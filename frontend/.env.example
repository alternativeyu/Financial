# 复制为 .env.local 后保存（.env.local 已被 git 忽略）
#
# 开发默认：npm run dev 时请求走 Vite 代理到本机后端（见 vite.config.js 中 VITE_PROXY_TARGET，默认 28480）
# VITE_PROXY_TARGET=http://127.0.0.1:28480
#
# 若要浏览器直连后端或网关（不用代理），在 .env.local 写：
# VITE_DEV_PROXY=0
# VITE_API_HOST=http://127.0.0.1:28480
# 经网关时：
# VITE_API_HOST=http://127.0.0.1:28481
