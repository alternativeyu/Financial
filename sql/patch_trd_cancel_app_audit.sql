-- 标准证券业务：投资者在 App 委托列表中对「未成交」委托发起撤单，
-- 由交易网关自动向交易所申报，柜台一般不做逐笔人工点撤。
--
-- 本仓库后端：`POST /api/app/trade/orders/{orderNo}/cancel` 走同步撤单模拟全链路，
-- 与上述「正常系统」一致，无需为「人工审核撤单」执行本文件中的任何 DDL。
--
-- 若曾误执行过含 app_user_id / client_request_seq_no 的实验脚本，多出的列可保留，
-- 当前 Java 代码不依赖这些列。
